
@code {
    [Inject]
    private HttpClient httpClient { get; set; }
    private record itemDelete(int id);
    private string message { get; set; }
    public async void delete(int id)
    {
        var itemDelete = await httpClient.DeleteFromJsonAsync<itemDelete>($"https://localhost:5001/api/Product/{id}");
       
      

    }
}
